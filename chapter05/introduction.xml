<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-tools-introduction">
  <?dbhtml filename="introduction.html"?>

  <title>Введение</title>

  <para>Это глава описывает процесс создания минимальной Linux системы. Эта система будет содержать только те инструменты, которые будут нужны для сборки конечной системы LFS в главе <xref linkend="chapter-building-system"/> и позволит работать с удобными настройками, чем обычная минимальная среда.</para>

  <para>Необходимо выполнить два шага для сборки минимальной системы. Первым шагом будет выполнена сборка независимого от хост системы набора инструментов (компилятор, ассемблер, компоновщик, библиотеки, и ещё несколько полезных утилит). Вторым шагом, с использованием этих инструментов будут скомпилированы остальные необходимые пакеты.</para>

  <para>Все пакеты, которые будут скомпилированы в этой главе, будут установлены в каталог <filename class="directory">$LFS/tools</filename> чтобы хранить их отдельно от хост системы и конечной системы LFS, которая будет создана в следующей главе. Как обсуждалось ранее, это набор инструментов - временный, и нам не требуется его наличие в системе LFS.</para>
</sect1>
