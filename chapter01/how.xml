<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-intro-how">
  <?dbhtml filename="how.html"?>

  <title>Как выполнить сборку системы LFS</title>

  <para>Сборка системы LFS будет выполнена с помощью уже установленного дистрибутива Linux 
  (например Debian, OpenMandriva, Fedora, или openSUSE). Именно эта система (хост) будет служить
  отправной точкой. Необходимые компоненты хоста, такие как компилятор, линковщик и командная оболочка 
  будут использованы для сборки новой системы. Выберите режим или опцию <quote>development (режим разработчика)</quote> 
  в процессе установки или конфигурирования хост системы чтобы использовать необходимые компоненты.</para>

  <para>Иным вариантом, может быть установка отдельного дистрибутива на вашу машину,
  вы можете использовать LiveCD коммерческого дистрибутива.</para>

  <para>В разделе <xref linkend="chapter-partitioning"/> описывается процесс создания новых разделов Linux. Это
  место, где будет выполняться компиляция, и куда будет установлена новая система LFS. В разделе <xref
  linkend="chapter-getting-materials"/> описывается процесс настройки рабочего окружения. В разделе <xref
  linkend="chapter-getting-materials"/> идут разъяснения о том, какие пакеты и патчи необходимо загрузить чтобы 
  выполнить сборку LFS, а также каким образом их хранить на файловой системе. В разделе <xref linkend="chapter-final-preps"/> обсуждается процесс настройки рабочего окружения.
  <xref linkend="chapter-final-preps"/> внимательно изучите раздел, так как в нем обсуждаются важные моменты,
  которые вам нужно знать до того, как вы приступите к процессу сборки.<xref linkend="chapter-temporary-tools"/>.</para>

  <para>Раздел <xref linkend="chapter-temporary-tools"/> содержит информацию о пакетах,
  которые будут использоваться для создания базовой среды для разработки (набор инструментов, тулчейн, toolchain), которые в свою очередь, будут использованы для последующей сборки окончательной системы LFS (<xref linkend="chapter-building-system"/>). Некоторые из пакетов требуют разрешения цикличной зависимости, например,
  чтобы выполнить сборку компилятора, вам нужен компилятор.</para>

  <para>Раздел <xref linkend="chapter-temporary-tools"/> также объясняет какие образом
  выполнить первый проход сборки набора инструментов, используя пакеты Binutils и GCC
  (в первом проходе , обычно два этих пакета будут переустановлены). Следующим шагом
  будет инструкция по сборке пакета Glibc - библиотеки C. Библиотека C будет 
  скомпилирована теми средствами, которые были установлены на этапе первого 
  прохода сборки временного набора инструментов. Далее, следует второй проход сборки 
  временного набора инструментов. На этот раз набор инструментов будет динамически слинкован 
  с собранной на предыдущем шаге библиотекой C. Оставшиеся пакеты <xref
  linkend="chapter-temporary-tools"/> будут использовать состояние 
  временного набора после второго прохода. Когда все будет готово, процесс установки 
  новой LFS системы больше не будет зависеть от 
  
  When this is done, the LFS installation process will no
  longer depend on the host distribution, with the exception of the running
  kernel. </para>

  <para>This effort to isolate the new system from the host distribution may
  seem excessive. A full technical explanation as to why this is done is provided in
  <xref linkend="ch-tools-toolchaintechnotes"/>.</para>

  <para>In <xref linkend="chapter-building-system"/>, the full LFS system is
  built. The <command>chroot</command> (change root) program is used to enter
  a virtual environment and start a new shell whose root directory will be
  set to the LFS partition. This is very similar to rebooting and instructing
  the kernel to mount the LFS partition as the root partition. The system
  does not actually reboot, but instead uses <command>chroot</command> because
  creating a bootable system requires additional work which is not necessary
  just yet. The major advantage is that <quote>chrooting</quote> allows you
  to continue using the host system while LFS is being built. While waiting
  for package compilations to complete, you can continue using your computer as
  normal.</para>

  <para>To finish the installation, the basic system configuration is set up in
  <xref linkend="chapter-bootscripts"/>, and the kernel and boot loader are set
  up in <xref linkend="chapter-bootable"/>. <xref linkend="chapter-finalizing"/>
  contains information on continuing the LFS experience beyond this book.
  After the steps in this book have been implemented, the computer will be
  ready to reboot into the new LFS system.</para>

  <para>This is the process in a nutshell. Detailed information on each
  step is discussed in the following chapters and package descriptions.
  Items that may seem complicated will be clarified, and everything will
  fall into place as you embark on the LFS adventure.</para>

</sect1>
